# RDK S100 智能机器人上位机系统 - 后端依赖
# 固定版本号，确保在不同机器上可重现

# Web 框架
fastapi==0.111.0
uvicorn[standard]==0.29.0
websockets==12.0
python-multipart==0.0.9

# 数据处理
pydantic==2.7.1
psutil==5.9.8

# 图像处理（ROS2 图像传输、视频帧编解码必需）
opencv-python-headless==4.9.0.80
numpy==1.26.4

# ROS2 Python 客户端（在 ROS2 环境中已内置，无需 pip 安装）
# rclpy  # 由 ROS2 Humble 提供

# 工具
python-dotenv==1.0.1
aiofiles==23.2.1
